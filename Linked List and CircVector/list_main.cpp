#include <iostream>

#include "circvector.h"
#include "linkedlist.h"

int main() {
  // Experimentation space!

  // // Create a LinkedList of integers
  // LinkedList<int> list;

  // // Add elements to the list
  // list.push_front(1);
  // list.push_back(2);
  // list.push_back(3);
  // list.push_front(0);

  // // Display the list
  // cout << "List after additions: " << list.to_string() << endl;

  // // Remove an element from the front
  // list.pop_front();
  // cout << "List after popping front: " << list.to_string() << endl;

  // // Remove an element from the back
  // list.pop_back();
  // cout << "List after popping back: " << list.to_string() << endl;

  // // Find an element in the list
  // size_t index = list.find(2);
  // cout << "Index of element 2: " << index << endl;

  // // Remove an element at a specific index
  // list.remove_at(0);
  // cout << "List after removing at index 0: " << list.to_string() << endl;

  // // Clear the list
  // list.clear();
  // cout << "List after clearing: " << list.to_string() << endl;

  // return 0;
}
